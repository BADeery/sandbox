{% extends "layouts/base.njk" %}

{% block content %}
  <article class="main-block">
    <section class="articles-page-intro">
        {{ content | safe}}
    </section>
    
    <div class="articles-page-grid">
      {% for post in collections.posts | reverse %}
        <article class="articles-page-card">
          <h2>
            <a href="{{ post.url }}">
              {% if post.data.icon %}
                <i data-lucide="{{ post.data.icon }}"></i>
              {% endif %}
              {{ post.data.title }}
            </a>
          </h2>

          {% if post.data.description %}
            <p class="articles-page-excerpt">{{ post.data.description }}</p>
          {% endif %}


          <div class="articles-page-flex-spacer"></div>

          <div class="articles-page-footer-meta-row">
            <span class="articles-page-post-date">
              <i data-lucide="calendar"></i> {{ post.date | auDate("dd-MMM-yyyy") }}
            </span>
            <span class="articles-page-card-read-time">
              <i data-lucide="clock"></i> {{ post.templateContent | readingTime }} read
            </span>
          </div>
        </article>
      {% endfor %}
    </div>
  </article>
{% endblock %}
